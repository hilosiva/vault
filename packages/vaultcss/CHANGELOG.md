# Changelog

## 0.3.5 (2026-05-12)

### Updated

- `lightningcss-plugin-fluid` の依存を `^0.2.1` に更新
  - `minCompSize` / `maxCompSize` オプション追加（カンプサイズの指定）
  - `mode: "snap"` でプロジェクト全体をスナップモードに
  - `snap` / `fit` / `free-max` / `free-min` / `free` キーワード対応
  - カンマなし構文（`fluid(16px 24px snap free-max)`）のバグ修正

## 0.3.4 (2026-05-12)

### Updated

- `lightningcss-plugin-fluid` の依存を `^0.2.0` に更新

## 0.3.3 以前

変更履歴は GitHub リリースを参照してください。
